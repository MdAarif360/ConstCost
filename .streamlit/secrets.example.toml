# Copy this file to .streamlit/secrets.toml for local testing.
# In Streamlit Community Cloud, paste the same value into app secrets.
DATABASE_URL = "postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"

# The app also recognizes Streamlit's SQL connection-style secrets:
# [connections.postgres]
# url = "postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"
